=== provenance (added by the run harness, not by the program) ===
program: verify.py
sha256:  36bfe417e3afc7cf11fb4dbc9777ee2e0bc65ea656c529103b03cc6f103aac9f
python:  Python 3.9.6
=== program output follows ===
verify.py -- Supermodular Log-Submodular Valuations Obey the Jaccard
             Triangle Inequality on Every Lattice
             (answers Badica-Badica arXiv:2608.18194v1, Sec. 7, open problem 2)
stdlib only; exact integer / Fraction / Z[sqrt5] arithmetic; python 3.9+

=== A. the three lattices printed in Section 3 ===
PASS m3_covers_define_a_lattice [|L|=5, 12 order relations, unique bottom 0, unique top 1, all 25 pairs have a unique join and a unique meet]
PASS n5_covers_define_a_lattice [|L|=5, 13 order relations, unique bottom 0, unique top 1, all 25 pairs have a unique join and a unique meet]
PASS b2_covers_define_a_lattice [|L|=4, 9 order relations, unique bottom 0, unique top 1, all 16 pairs have a unique join and a unique meet]
PASS m3_is_not_distributive [witness (x,y,z)=(a,b,c): x^(yvz)=a but (x^y)v(x^z)=0]
PASS n5_is_not_distributive [witness (x,y,z)=(c,a,b): x^(yvz)=c but (x^y)v(x^z)=b]
PASS m3_is_relatively_complemented [every element of every interval has a complement in that interval, so M_3 lacks exactly ONE of the two traits the open problem names (distributivity)]
PASS n5_is_not_relatively_complemented [no relative complement for b in the interval [0,c], so N_5 lacks BOTH traits]

=== B. the enumerator control, and the location of the first open size ===
PASS lattice_iso_class_counts_match_oeis_a006966 [n=2..6 -> 1, 1, 2, 5, 15 (OEIS A006966 a(2..6) = 1,1,2,5,15); naturally-labelled counts 1, 1, 2, 7, 39]
PASS every_lattice_of_order_at_most_four_is_settled_by_the_sources_own_theorems [5 of 5 isomorphism classes of order <= 4 are a chain (every valuation is then modular, so th:mod applies) or are distributive AND relatively complemented (so th:super-log-sub applies)]
PASS m3_and_n5_are_exactly_the_nondistributive_lattices_of_order_five [2 of the 5 isomorphism classes of order 5 are non-distributive, and they are isomorphic to the printed M_3 (True) and N_5 (True); so |L|=5 is the first order the source's two theorems leave open, and M_3, N_5 are that whole instance]

=== C. Steps 4 and 5 of the proof, as exact identities in Z[p,P,q,Q,b] ===
PASS step4_clearing_denominators_is_exactly_the_product_inequality [pQ+qP-PQ-b(p+q-b) = -[(P-p)(Q-q)-(b-p)(b-q)] identically; both sides expand to +1pQ-1pb+1Pq-1PQ-1qb+1bb, so p/P+q/Q <= 1+b(p+q-b)/(PQ) IS (P-p)(Q-q) >= (b-p)(b-q)]
PASS step5_nonnegativity_certificate_is_an_identity [(P-p)(Q-q)-(b-p)(b-q) = (P-b)(Q-q)+(b-p)(Q-b) identically; monotonicity gives P>=b, Q>=b, b>=p, b>=q, so every one of the four factors is >= 0 and the sum is >= 0 termwise -- no case analysis, no ordering assumption beyond p,q <= b <= P,Q]
PASS step4_and_step5_compose_to_the_triangle_inequality [adding the two identities gives pQ+qP-PQ-b(p+q-b) = -[(P-b)(Q-q)+(b-p)(Q-b)] <= 0, which is the cleared form of S(A,B)+S(B,C) <= 1 + b(p+q-b)/(PQ) <= 1 + S(A,C)]

=== D. the four order facts Lemma 1 and Lemma 2 use, on every lattice |L| <= 6 ===
    (Lemma 1 = the paper's Step 1, its equation (6) r >= p+q-b; Lemma 2 = the paper's Step 2, its equation (7) R <= PQ/b. The paper numbers these as STEPS, not as lemmas.)
PASS lemma1_order_side_conditions_hold_at_every_triple [(A^B)v(B^C) <= B at 4029/4029 triples and A^B^C <= A^C at 4029/4029 triples, over all 25 isomorphism classes of order <= 6]
PASS lemma2_order_side_conditions_hold_at_every_triple [(AvB)^(BvC) >= B at 4029/4029 triples and AvC <= AvBvC at 4029/4029 triples]

=== E. the two forced positives: drop either hypothesis and the TI breaks ===
PASS control_a1_without_log_submodularity [f=(2,3,3,20) on B_2 fails exactly LOGSUBMODULAR; margin at (x,0,y) is 7/30 (paper: 7/30) > 0, so the triangle inequality is VIOLATED, and the step that breaks is LEMMA2 (paper: LEMMA2)]
PASS control_a2_without_supermodularity [f=(1,3,3,4) on B_2 fails exactly SUPERMODULAR; margin at (x,1,y) is 1/4 (paper: 1/4) > 0, so the triangle inequality is VIOLATED, and the step that breaks is LEMMA1 (paper: LEMMA1)]
PASS control_b1_modular_valuation_is_admissible [f=(1,2,3,4) on B_2 is admissible and the largest cleared margin over all 64 triples is exactly 0, so the triangle inequality holds everywhere with equality somewhere]
PASS control_b2_constant_valuation_is_admissible [f=(1,1,1,1) on B_2 is admissible and the largest cleared margin over all 64 triples is exactly 0, so the triangle inequality holds everywhere with equality somewhere]

=== F. the finite sweep of Section 3 (a control, not the proof) ===
PASS sweep_m3_no_violation [39 admissible valuations on the printed M3 over the grid 1,2,3,4,6,9 with f(0)=1: largest cleared margin 0, triangle-inequality violations 0, lemma failures 0]
PASS sweep_n5_no_violation [47 admissible valuations on the printed N5 over the grid 1,2,3,4,6,9 with f(0)=1: largest cleared margin 0, triangle-inequality violations 0, lemma failures 0]
PASS sweep_orders_four_to_six_reproduces_the_papers_three_totals [22 isomorphism classes, 1814 admissible valuations (paper: 1814), largest cleared margin 0 (paper: 0), violations 0, lemma failures 0]

=== G. the near-miss valuation g of Section 4, exactly in Z[sqrt5] ===
PASS near_miss_g_is_monotone [g(0)=5/2 - 1/2*sqrt5 <= g(x)=g(y)=2 <= g(1)=5/2 + 1/2*sqrt5]
PASS near_miss_g_is_supermodular [g(1)+g(0) = 5 >= 4 = g(x)+g(y) on the only incomparable pair; smallest supermodularity slack over all 16 pairs is 0]
PASS near_miss_g_has_supermodular_reciprocal [1/g(1)+1/g(0) = (g(1)+g(0))/(g(1)g(0)) = 5/5 = 1 >= 1/g(x)+1/g(y) = 1, with EQUALITY, and no pair fails; so g satisfies the source's necessary condition th:super-nec-cond in full]
PASS near_miss_g_is_not_log_submodular [g(1)*g(0) = 5 > 4 = g(x)*g(y), so g is OUTSIDE the hypothesis class of the theorem proved here]
PASS near_miss_g_satisfies_the_triangle_inequality_with_equality [largest cleared margin over all 64 triples is 0 (sign 0 <= 0); at (x,0,y) it is exactly 0, and d_g(x,y) = -1/2 + 1/2*sqrt5 = (sqrt5-1)/2 = d_g(x,0)+d_g(0,y) = 2*(-1/4 + 1/4*sqrt5). So log-submodularity is NOT necessary, the sufficient class here is STRICTLY smaller than the necessary class of th:super-nec-cond, and the source's open problem 4 (line 635) stays open in both directions]

NOTE NOT RE-RUN: the theorem of this paper quantifies over ALL lattices and all strictly positive monotone supermodular log-submodular valuations, and no program can range over that class. What is machine-checked above is (i) the two algebraic identities that carry Steps 4 and 5, as exact polynomial identities, and (ii) the order facts of Lemmas 1 and 2, on lattices of order <= 6 only. The finite sweep of section F is a CONTROL over orders 4,5,6 on a six-value integer grid; it is NOT an exhaustion of the admissible cone, and "largest margin 0" means "nothing beat the equality case", not "the cone was searched".
NOTE NOT RE-RUN: the source paper's own theorems (th:mod, th:super-log-sub, th:super-nec-cond) are quoted, not re-proved; and Step 3 of the proof (the sign split on p+q-b) plus the statement that d_{f,J} is in general only a PSEUDOmetric are hand arguments with no computational content. Nothing above touches the source's open problems 1, 3 or 4, or any literature claim.

VERDICT: ALL 27 CHECKS PASS
=== program exited with status 0 ===
